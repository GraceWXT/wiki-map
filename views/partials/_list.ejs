<section>
  <header><span id="list-header">${list-header}</span></header>
  <ul>
    <!-- Test <li> to build the structure and test css rules -->
    <li>
      <span>${Best Brunch Spots in New York}</span>
      <form action="/favs/${map_id}" method="POST">
        <button type="submit" class="fav-button">
          <i class="fa-solid fa-heart fav-icon" id="${map_id}"></i>
        </button>
      </form>
    </li>
    <li>
      <span>${Bad-Guy Locations}</span>
      <form action="/favs/${map_id}" method="POST">
        <button type="submit" class="fav-button">
          <i class="fa-solid fa-heart fav-icon" id="${map_id}"></i>
        </button>
      </form>
    </li>
  </ul>
</section>
